# Structured Output Template

Return valid JSON only with this exact schema:
{
  "summary": "string",
  "assumptions": ["string"],
  "recommended_actions": ["string"],
  "risks": ["string"]
}

Task:
{{user_input}}
